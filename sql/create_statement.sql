CREATE TABLE feedme.main_activity (

    athlete_id BIGINT,
    platform_id VARCHAR(20),
    resource_state INTEGER,
    activity_name VARCHAR(255),
    distance_mt  DECIMAL,
    moving_time_sec INTEGER,
    elapsed_time_sec INTEGER,
    total_elevation_gain_mt DECIMAL,
    activity_type VARCHAR(100),
    activity_id BIGINT,
    external_id VARCHAR(255),
    upload_id BIGINT,
    start_date TIMESTAMP,
    start_date_local TIMESTAMP,
    timezone VARCHAR(100),
    utc_offset DECIMAL,
    start_lat DECIMAL,
    start_lng DECIMAL,
    end_lat DECIMAL,
    end_lng DECIMAL,
    achievement_count INTEGER,
    kudos_count INTEGER,
    comment_count INTEGER,
    athlete_count INTEGER,
    photo_count INTEGER,
   trainer VARCHAR(10),
   commute VARCHAR(10),
   man_ual VARCHAR(10),
   priv_ate VARCHAR(10),
   visibility VARCHAR(25),
   flagged VARCHAR(10),
   gear_id VARCHAR(50),
   from_accepted_tag VARCHAR(10),
   upload_id_str VARCHAR(50),
   average_speed DECIMAL,
   max_speed DECIMAL,
   average_cadence DECIMAL,
   average_watts DECIMAL,
   weighted_average_watts DECIMAL,
   kilojoules DECIMAL,
   device_watts VARCHAR(10),
   has_heartrate VARCHAR(10),
   heartrate_opt_out VARCHAR(10),
   display_hide_heartrate_option VARCHAR(10),
   max_watts DECIMAL,
   elev_high DECIMAL,
   elev_low DECIMAL,
   pr_count INTEGER,
   total_photo_count INTEGER,
   has_kudoed VARCHAR(10),
   description VARCHAR(255),
   calories DECIMAL,
   perceived_exertion VARCHAR(100),
   prefer_perceived_exertion VARCHAR(10),
   device_name VARCHAR(100),
   embed_token VARCHAR(255)
)